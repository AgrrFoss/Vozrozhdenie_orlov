var sliderGallery = new Swiper('.gallery', {
    
    navigation: {
      nextEl: '.swiper-button-next',
      prevEl: '.swiper-button-prev',
    },
  });

  const sliderFeedback = new Swiper ('.swiper-feedbacks')